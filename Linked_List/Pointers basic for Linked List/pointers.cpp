#include <iostream>
using namespace std;
int main()
{
    // int a=10;
    // int*  ptr=&a;  // pointer is a variable that holds the address of other variable
    // int** ptr2=&ptr;

    // cout<<&a<<endl;
    // cout<<ptr<<endl;
    // cout<<*ptr2<<endl;   // derefernce opeartor is used to extract the value at that memeory address it is holding

    // int b=323;
    // int* ptr3=nullptr;
    // int* ptr4=&b;
    // ptr3=ptr4;

    // *ptr3=423434;
    // cout<<ptr3<<endl;         // if one pointer is assigned to other pointer then both pointers point to same memory
    // cout<<*ptr4<<endl;

   
    return 0;
}